# Cloudflare Pages 路由规则（顺序敏感，先匹配先生效）
# 1) /api/* 反向代理到后端（状态码 200 = 代理而非跳转），避免跨域 CORS
/api/*  https://xy-api.skyed.dpdns.org/api/:splat  200
# 2) 其余路径走 SPA 兜底，交给前端路由（react-router）处理
/*  /index.html  200
